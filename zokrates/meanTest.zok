def main(u32[2][22] a, u32 mean, u32 accuracy) -> u32:
    u32 res = 0
        for u32 j in 0..20 do
            u32 r = mean - a[1][j]
            res = res + (r*r)
        endfor 
        res = res/(20 - 1) / accuracy
    return (res)